{%- comment -%}
  Opening chrome for pages that render inside layout/theme.liquid without a
  section of ours: Shopify's /policies/ pages and app-proxy pages.

  It is the landing page's own header, so those pages are not a different
  site with the same logo. Styled by assets/jw-v5.css, which the layout
  loads on every page that renders this.
{%- endcomment -%}
{% render 'jw-header' %}
